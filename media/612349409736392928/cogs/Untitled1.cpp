#include <stdio.h>

int main(){
	int num, sum, rem, n;
	printf("Done");
	for(n=100;n<1000;n++)
	{
		num = n;
		sum = 0;
		printf("Done");
		while(num!=0)
		{
			rem = num%10;
		if (n==sum)
		{
			printf("%d is an armstrong number", n);
			
		}
	}
	}
	printf("Done");
	return 0;
}
